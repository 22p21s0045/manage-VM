---
- name: Deploy Demo Project
  hosts: all
  become: true
  vars:
    project_repo: https://github.com/22p21s0045/int531-demo-project.git
    project_dest: /home/{{ ansible_user }}/int531-demo-project

  tasks:
    - name: Install git
      apt:
        name: git
        state: present

    - name: Clone demo project from GitHub
      git:
        repo: "{{ project_repo }}"
        dest: "{{ project_dest }}"
        clone: yes
        update: yes
      become_user: "{{ ansible_user }}"

    - name: Run docker compose
      shell: docker compose up -d
      args:
        chdir: "{{ project_dest }}"
      ignore_errors: yes
